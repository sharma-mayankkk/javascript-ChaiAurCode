const id = 'MayankSharma' // const is a keyword which is used to define constants which you can't change earlier
let password = 122333444 // let is a keyword used to define variables
var city = "Bhopal" // var has some drawbacks so avoid using it 
emial_Id = "sharma@gmail.com" // we can also define a variable without any keyword but this is not a good practice
let palacement  // we can left the variable without giving it a value 

console.log(id)  //by this method we can print output for a single variable
console.table([id, password, city, emial_Id, palacement]) // by this method we can print output for the multiple variable in the tabular form:

// ************************************** CONVERSION *********************************************

let score = "33abc"
let valueInNumber = Number(score)
console.log(typeof(score)) 


// "33" => 33
// "33abc" => NaN
//  true => 1; false => 0

// 1 => true; 0 => false
// "" => false
// "hitesh" => tru